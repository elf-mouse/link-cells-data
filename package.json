{
  "name": "link-cells-data",
  "scripts": {
    "update": "python main.py",
    "prod": "echo 'Awesome data updated!'",
    "deploy": "balm-git-prod"
  },
  "private": true,
  "scripts": {
    "build": "parcel build index.html --dist-dir dist"
  }
}
  "devDependencies": {
    "parcel": "^2.15.4"
  }
}
